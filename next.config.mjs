/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode:false,
    images: {
        domains: ['media.graphassests.com','ap-south-1.graphassets.com']
    }
};

export default nextConfig;
